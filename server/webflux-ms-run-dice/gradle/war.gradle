apply plugin: "war"

bootWar {
    mainClass = "game.ceelo.server.microservice.run.dice.RunDiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
