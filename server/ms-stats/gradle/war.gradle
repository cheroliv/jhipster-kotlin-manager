apply plugin: "war"

bootWar {
    mainClass = "game.ceelo.server.ms.stats.StatsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
